#!/bin/bash

source .config

read -p "Remove $BASE_PATH [y/N]? " choice
[[ $choice == 'y' ]] && sudo rm -fr $BASE_PATH

#echo "Removing $TEAM_REPO_BASE_DIR/*"
#sudo rm -fr $TEAM_REPO_BASE_DIR/*
#cd webapp
#rake db:setup
